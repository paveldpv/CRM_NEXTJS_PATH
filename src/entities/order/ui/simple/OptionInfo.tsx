import React from 'react'

type Props = {
	optionsDescription?: string[]
	processCompleted?: number
}

export default function OptionInfo({optionsDescription,processCompleted}: Props) {
	return (
		<div>OptionInfo</div>
	)
}